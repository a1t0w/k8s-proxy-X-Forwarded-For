apiVersion: v1
kind: Service
metadata:
  name: nginx3-svc
  namespace: nginx-forward
  labels:
    app: nginx3
spec:
  type: ClusterIP
  selector:
    app: nginx3
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
