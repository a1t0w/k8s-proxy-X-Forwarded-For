apiVersion: v1
kind: Service
metadata:
  name: nginx2-svc
  namespace: nginx-forward
  labels:
    app: nginx2
spec:
  type: ClusterIP
  selector:
    app: nginx2
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
